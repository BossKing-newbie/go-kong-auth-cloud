_format_version: "2.1"
_transform: true

services:
- name: 'my-service'
  url: 'http://mockbin.org'
  routes:
  - name: 'my-route'
    paths:
      - '/auth'
  plugins:
  - name: go-hello
    config:
      message: "hello world!"